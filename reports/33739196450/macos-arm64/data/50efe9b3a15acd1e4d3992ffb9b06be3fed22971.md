# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: golden-path.spec.ts >> Release 生产安装包 E2E 黄金主链路验证 >> 04. 文件探测与列表/详情展示 (File Explorer & Details Panel)
- Location: pro/tests/e2e/release-pkg/suites/golden-path.spec.ts:234:7

# Error details

```
Error: expect(received).toBeGreaterThan(expected)

Expected: > 0
Received:   0
```

# Test source

```ts
  154 |         )
  155 |         .first()
  156 |       if (await nextBtn.isVisible().catch(() => false)) {
  157 |         await nextBtn.click().catch(() => {})
  158 |       }
  159 |     } else {
  160 |       console.log('[Step 02] 无向导蒙层或已直接进入主界面。')
  161 |     }
  162 | 
  163 |     // 给页面初始化与授权探测预留稳定时间
  164 |     await new Promise(r => setTimeout(r, 4000))
  165 |     page = await app.getPage()
  166 |     await page.waitForLoadState('domcontentloaded').catch(() => {})
  167 | 
  168 |     // 断言主应用界面容器已挂载
  169 |     const rootElement = page.locator('#root, body, main').first()
  170 |     await expect(rootElement).toBeVisible({ timeout: 20000 })
  171 |     await page
  172 |       .screenshot({ path: path.join(__dirname, '../reports/html/step-02-main-ready.png') })
  173 |       .catch(() => {})
  174 |   })
  175 | 
  176 |   test('03. 挂载测试快照工作区 (Mount Golden Workspace)', async () => {
  177 |     console.log(`--- [Step 03] 挂载测试快照工作区: ${workspaceDir} ---`)
  178 |     page = await app.getPage()
  179 |     await page.waitForLoadState('domcontentloaded').catch(() => {})
  180 | 
  181 |     // 通过 electronAPI 添加工作目录、设置当前工作区并触发同步
  182 |     await page.evaluate(async wsPath => {
  183 |       const api = (window as any).electronAPI
  184 |       if (!api) throw new Error('electronAPI 未定义')
  185 | 
  186 |       // 1. 添加工作区
  187 |       if (typeof api.addWorkspaceDirectory === 'function') {
  188 |         await api.addWorkspaceDirectory({
  189 |           name: 'SPEEDY-Golden-Workspace',
  190 |           path: wsPath,
  191 |           type: 'SPEEDY'
  192 |         })
  193 |       }
  194 | 
  195 |       // 2. 切换当前工作区
  196 |       if (typeof api.setCurrentWorkspaceDirectory === 'function') {
  197 |         await api.setCurrentWorkspaceDirectory(wsPath)
  198 |       }
  199 | 
  200 |       // 3. 广播前端更新事件
  201 |       window.dispatchEvent(new CustomEvent('workspace-directories-updated'))
  202 |     }, workspaceDir)
  203 | 
  204 |     // 等待 UI 响应并刷新工作区
  205 |     await new Promise(r => setTimeout(r, 3500))
  206 |     page = await app.getPage()
  207 | 
  208 |     // 验证工作区列表已成功记录（支持慢 IO 异步写入短轮询）
  209 |     let directories: any[] = []
  210 |     for (let attempt = 0; attempt < 10; attempt++) {
  211 |       directories = await page.evaluate(async () => {
  212 |         const api = (window as any).electronAPI
  213 |         return api && typeof api.getAllWorkspaceDirectories === 'function'
  214 |           ? await api.getAllWorkspaceDirectories()
  215 |           : []
  216 |       })
  217 |       if (directories.length >= 1) break
  218 |       await new Promise(r => setTimeout(r, 600))
  219 |     }
  220 | 
  221 |     console.log(`[Step 03] 当前已挂载工作区数量: ${directories.length}`)
  222 |     expect(directories.length).toBeGreaterThanOrEqual(1)
  223 | 
  224 |     const currentDir = directories.find(
  225 |       (d: any) => d.path === workspaceDir || d.path.toLowerCase() === workspaceDir.toLowerCase()
  226 |     )
  227 |     expect(currentDir).toBeDefined()
  228 | 
  229 |     await page
  230 |       .screenshot({ path: path.join(__dirname, '../reports/html/step-03-workspace-mounted.png') })
  231 |       .catch(() => {})
  232 |   })
  233 | 
  234 |   test('04. 文件探测与列表/详情展示 (File Explorer & Details Panel)', async () => {
  235 |     console.log('--- [Step 04] 验证文件列表展示与详情面板交互 ---')
  236 |     page = await app.getPage()
  237 | 
  238 |     // 主动触发目录读取并获取快照内的文件结构
  239 |     const dirContent = await page.evaluate(async wsPath => {
  240 |       const api = (window as any).electronAPI
  241 |       if (api && typeof api.readDirectory === 'function') {
  242 |         return await api.readDirectory(wsPath)
  243 |       }
  244 |       return { files: [], directories: [] }
  245 |     }, workspaceDir)
  246 | 
  247 |     const scannedFiles = dirContent.files || []
  248 |     const scannedDirs = dirContent.directories || []
  249 |     console.log(
  250 |       `[Step 04] 后端扫描到的文件数: ${scannedFiles.length}, 子目录数: ${scannedDirs.length}`
  251 |     )
  252 | 
  253 |     // 验证扫描到的文件数不为 0
> 254 |     expect(scannedFiles.length + scannedDirs.length).toBeGreaterThan(0)
      |                                                      ^ Error: expect(received).toBeGreaterThan(expected)
  255 |     console.log(`[Step 04] 文件名样本: ${scannedFiles.map((f: any) => f.name).join(', ')}`)
  256 | 
  257 |     // 等待 UI 渲染
  258 |     await new Promise(r => setTimeout(r, 2000))
  259 | 
  260 |     // 检查页面渲染的文件元素或文件名文本
  261 |     const fileElements = page.locator(
  262 |       'div:has-text("成都市"), div:has-text("项目模块"), span:has-text("通知"), span:has-text("需求"), [class*="truncate"], tbody tr, [class*="card"]'
  263 |     )
  264 |     const domFileCount = await fileElements.count().catch(() => 0)
  265 |     console.log(`[Step 04] 页面匹配到的文件相关 DOM 元素数量: ${domFileCount}`)
  266 | 
  267 |     // 验证页面至少能匹配到主工作区与文件相关元素
  268 |     expect(domFileCount).toBeGreaterThan(0)
  269 | 
  270 |     // 尝试点击第一个文件项以唤起详情交互
  271 |     await fileElements
  272 |       .first()
  273 |       .click()
  274 |       .catch(() => {})
  275 |     await new Promise(r => setTimeout(r, 1000))
  276 | 
  277 |     // 验证主视图区域可见
  278 |     const mainView = page
  279 |       .locator('main, [class*="fileExplorer"], [class*="layout"], [class*="content"]')
  280 |       .first()
  281 |     await expect(mainView).toBeVisible()
  282 | 
  283 |     await page
  284 |       .screenshot({ path: path.join(__dirname, '../reports/html/step-04-file-explorer.png') })
  285 |       .catch(() => {})
  286 |   })
  287 | 
  288 |   test('05. 单个文件分析结果与属性面板值属性精确检测 (File Analysis & Attributes Inspection)', async () => {
  289 |     console.log(
  290 |       '--- [Step 05] 针对《项目模块_功能需求文档_日历调度AI集成需求_V1.docx》进行分析结果面板与AI深度推理全面校验 ---'
  291 |     )
  292 |     page = await app.getPage()
  293 | 
  294 |     // 1. 前置：检查并确保 AI 服务已就绪
  295 |     console.log('[Step 05] 正在检查 AI 服务初始化与引擎就绪状态...')
  296 |     const aiStatusBefore = await page.evaluate(async () => {
  297 |       const api = (window as any).electronAPI
  298 |       if (!api || !api.aiService) return { supported: false, status: 'unknown' }
  299 |       try {
  300 |         const status =
  301 |           typeof api.aiService.getStatus === 'function'
  302 |             ? await api.aiService.getStatus()
  303 |             : 'unknown'
  304 |         const isInit =
  305 |           typeof api.aiService.isInitialized === 'function'
  306 |             ? await api.aiService.isInitialized()
  307 |             : false
  308 |         const initInfo =
  309 |           typeof api.aiService.getInitializationInfo === 'function'
  310 |             ? await api.aiService.getInitializationInfo()
  311 |             : null
  312 |         const capabilities =
  313 |           typeof api.aiService.getCapabilities === 'function'
  314 |             ? await api.aiService.getCapabilities()
  315 |             : null
  316 | 
  317 |         // 如果未就绪且未初始化，主动触发一次显式初始化
  318 |         if (!isInit && typeof api.aiService.initialize === 'function') {
  319 |           console.log('[E2E Page] 正在主动触发 api.aiService.initialize()...')
  320 |           await api.aiService
  321 |             .initialize()
  322 |             .catch((err: any) => ({ success: false, error: err?.message }))
  323 |         }
  324 | 
  325 |         return {
  326 |           supported: true,
  327 |           status,
  328 |           isInit,
  329 |           initInfo,
  330 |           capabilities
  331 |         }
  332 |       } catch (err: any) {
  333 |         return { supported: true, error: err?.message || String(err) }
  334 |       }
  335 |     })
  336 |     console.log('[Step 05] AI 服务前置探测状态:', JSON.stringify(aiStatusBefore, null, 2))
  337 | 
  338 |     // 等待 AI 服务达到可用态 (idle 或 processing)，最长等待 45 秒 (应对低配 CI 冷启动)
  339 |     const aiReadyStart = Date.now()
  340 |     let currentAIStatus = 'unknown'
  341 |     while (Date.now() - aiReadyStart < 45000) {
  342 |       currentAIStatus = await page
  343 |         .evaluate(async () => {
  344 |           const api = (window as any).electronAPI
  345 |           return api?.aiService?.getStatus ? await api.aiService.getStatus() : 'ready'
  346 |         })
  347 |         .catch(() => 'unknown')
  348 | 
  349 |       if (
  350 |         currentAIStatus === 'idle' ||
  351 |         currentAIStatus === 'processing' ||
  352 |         currentAIStatus === 'ready'
  353 |       ) {
  354 |         console.log(
```