# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: golden-path.spec.ts >> Release 生产安装包 E2E 黄金主链路验证 >> 05. 单个文件分析结果与属性面板值属性精确检测 (File Analysis & Attributes Inspection)
- Location: pro/tests/e2e/release-pkg/suites/golden-path.spec.ts:304:7

# Error details

```
Error: page.evaluate: Error: Error invoking remote method 'database/execute-get': SqliteError: no such column: file_path
```

# Test source

```ts
  328 |         const capabilities =
  329 |           typeof api.aiService.getCapabilities === 'function'
  330 |             ? await api.aiService.getCapabilities()
  331 |             : null
  332 | 
  333 |         // 如果未就绪且未初始化，主动触发一次显式初始化
  334 |         if (!isInit && typeof api.aiService.initialize === 'function') {
  335 |           console.log('[E2E Page] 正在主动触发 api.aiService.initialize()...')
  336 |           await api.aiService
  337 |             .initialize()
  338 |             .catch((err: any) => ({ success: false, error: err?.message }))
  339 |         }
  340 | 
  341 |         return {
  342 |           supported: true,
  343 |           status,
  344 |           isInit,
  345 |           initInfo,
  346 |           capabilities
  347 |         }
  348 |       } catch (err: any) {
  349 |         return { supported: true, error: err?.message || String(err) }
  350 |       }
  351 |     })
  352 |     console.log('[Step 05] AI 服务前置探测状态:', JSON.stringify(aiStatusBefore, null, 2))
  353 | 
  354 |     // 等待 AI 服务达到可用态 (idle 或 processing)，最长等待 45 秒 (应对低配 CI 冷启动)
  355 |     const aiReadyStart = Date.now()
  356 |     let currentAIStatus = 'unknown'
  357 |     while (Date.now() - aiReadyStart < 45000) {
  358 |       currentAIStatus = await page
  359 |         .evaluate(async () => {
  360 |           const api = (window as any).electronAPI
  361 |           return api?.aiService?.getStatus ? await api.aiService.getStatus() : 'ready'
  362 |         })
  363 |         .catch(() => 'unknown')
  364 | 
  365 |       if (
  366 |         currentAIStatus === 'idle' ||
  367 |         currentAIStatus === 'processing' ||
  368 |         currentAIStatus === 'ready'
  369 |       ) {
  370 |         console.log(
  371 |           `[Step 05] AI 服务已处于可用状态 (${currentAIStatus})，耗时 ${Date.now() - aiReadyStart}ms`
  372 |         )
  373 |         break
  374 |       }
  375 |       await new Promise(r => setTimeout(r, 1500))
  376 |     }
  377 | 
  378 |     const targetFileName = '项目模块_功能需求文档_日历调度AI集成需求_V1.docx'
  379 |     console.log(`[Step 05] 目标测试文件: ${targetFileName}`)
  380 | 
  381 |     // 2. 将工作区目标文件准确加入分析队列并启动分析
  382 |     console.log(`[Step 05] 正在确保目标文件《${targetFileName}》入队并触发分析队列...`)
  383 |     const enqueueResult = await page.evaluate(
  384 |       async (params: { wsPath: string; fileName: string }) => {
  385 |         const api = (window as any).electronAPI
  386 |         if (!api || !api.getDatabase) return { success: false, reason: 'electronAPI 不可用' }
  387 |         const db = api.getDatabase()
  388 | 
  389 |         // 仅查询当前测试关注的目标物理文件，避免多个文件同时排队瓜分 CI 虚拟机 CPU 算力
  390 |         const files = await db
  391 |           .prepare('SELECT * FROM workspace_files WHERE (path LIKE ? OR name LIKE ?) AND name LIKE ?')
  392 |           .all(`%${params.wsPath}%`, `%${params.fileName}%`, `%${params.fileName}%`)
  393 | 
  394 |         if (!files || files.length === 0) {
  395 |           return { success: false, reason: '工作区未检索到物理文件' }
  396 |         }
  397 | 
  398 |         const queueItems = files.map((f: any) => ({
  399 |           path: f.path,
  400 |           name: f.name,
  401 |           size: f.size || 0,
  402 |           type: f.path.split('.').pop() || 'file'
  403 |         }))
  404 | 
  405 |         // 显式添加到分析队列并启动分析
  406 |         if (typeof api.addToAnalysisQueue === 'function') {
  407 |           await api.addToAnalysisQueue(queueItems, true)
  408 |         }
  409 |         if (typeof api.startAnalysis === 'function') {
  410 |           await api.startAnalysis()
  411 |         }
  412 | 
  413 |         return { success: true, count: queueItems.length }
  414 |       },
  415 |       { wsPath: workspaceDir, fileName: targetFileName }
  416 |     )
  417 | 
  418 |     console.log('[Step 05] 分析队列触发结果:', enqueueResult)
  419 | 
  420 |     // 3. 轮询等待目标文件进入分析队列并完成分析 (is_analyzed === 1 且 file_fingerprint 存在)
  421 |     console.log(`[Step 05] 正在轮询等待《${targetFileName}》分析完成 (is_analyzed = 1)...`)
  422 |     let analysisRecord: any = null
  423 |     const maxWaitMs = 240000 // CI 虚拟机环境纯 CPU 本地模型端到端推理预留充足时间 (240s)
  424 |     const pollStart = Date.now()
  425 |     let lastLogTime = 0
  426 | 
  427 |     while (Date.now() - pollStart < maxWaitMs) {
> 428 |       analysisRecord = await page.evaluate(
      |                                   ^ Error: page.evaluate: Error: Error invoking remote method 'database/execute-get': SqliteError: no such column: file_path
  429 |         async (params: { wsPath: string; fileName: string }) => {
  430 |           const api = (window as any).electronAPI
  431 |           if (!api || !api.getDatabase) return null
  432 |           const db = api.getDatabase()
  433 | 
  434 |           // 1. 查询物理文件记录
  435 |           const wf = await db
  436 |             .prepare('SELECT * FROM workspace_files WHERE name LIKE ? OR path LIKE ? LIMIT 1')
  437 |             .get(`%${params.fileName}%`, `%${params.fileName}%`)
  438 | 
  439 |           if (!wf) return null
  440 | 
  441 |           // 若尚未完成分析或指纹尚未生成，保持等待
  442 |           if (wf.is_analyzed !== 1 || !wf.file_fingerprint) {
  443 |             return {
  444 |               file: wf,
  445 |               tags: [],
  446 |               isCompleted: false
  447 |             }
  448 |           }
  449 | 
  450 |           // 2. 联查 files 与 file_contents
  451 |           let fileData = { ...wf }
  452 |           const contentRow = await db
  453 |             .prepare(
  454 |               'SELECT * FROM files LEFT JOIN file_contents USING(file_fingerprint) WHERE file_fingerprint = ?'
  455 |             )
  456 |             .get(wf.file_fingerprint)
  457 |           if (contentRow) {
  458 |             fileData = { ...fileData, ...contentRow }
  459 |           }
  460 | 
  461 |           // 3. 联查关联的维度标签
  462 |           const tags = await db
  463 |             .prepare(
  464 |               `
  465 |             SELECT t.id, t.name, d.id as dim_id, d.name as dim_name
  466 |             FROM file_tag_relations r
  467 |             JOIN file_tags t ON r.tag_id = t.id
  468 |             LEFT JOIN file_dimensions d ON t.dimension_id = d.id
  469 |             WHERE r.file_fingerprint = ?
  470 |           `
  471 |             )
  472 |             .all(wf.file_fingerprint)
  473 | 
  474 |           // 4. 查询分析队列是否有报错信息
  475 |           const queueError = await db
  476 |             .prepare('SELECT * FROM analysis_queue WHERE file_path LIKE ? ORDER BY id DESC LIMIT 1')
  477 |             .get(`%${params.fileName}%`)
  478 | 
  479 |           return {
  480 |             file: fileData,
  481 |             tags: tags || [],
  482 |             queueItem: queueError || null,
  483 |             isCompleted: true
  484 |           }
  485 |         },
  486 |         { wsPath: workspaceDir, fileName: targetFileName }
  487 |       )
  488 | 
  489 |       if (analysisRecord && analysisRecord.isCompleted) {
  490 |         console.log(`[Step 05] 目标文件在 ${Date.now() - pollStart}ms 内完成处理！`)
  491 |         break
  492 |       }
  493 | 
  494 |       if (Date.now() - lastLogTime > 15000) {
  495 |         lastLogTime = Date.now()
  496 |         console.log(
  497 |           `[Step 05 轮询中...] 耗时 ${Math.round((Date.now() - pollStart) / 1000)}s / 240s, 目标状态: is_analyzed=${analysisRecord?.file?.is_analyzed ?? 'unknown'}, fingerprint=${analysisRecord?.file?.file_fingerprint ? '已就绪' : '等待中'}`
  498 |         )
  499 |       }
  500 | 
  501 |       await new Promise(r => setTimeout(r, 1500))
  502 |     }
  503 | 
  504 |     console.log('[Step 05] 数据库提取的最终分析记录:', JSON.stringify(analysisRecord, null, 2))
  505 | 
  506 |     // 如果未完成或有错误，打印深入诊断信息
  507 |     if (!analysisRecord || !analysisRecord.isCompleted) {
  508 |       const dbDiagnostic = await page
  509 |         .evaluate(async () => {
  510 |           const api = (window as any).electronAPI
  511 |           if (!api || !api.getDatabase) return 'electronAPI 不可用'
  512 |           const db = api.getDatabase()
  513 |           const queueRows = await db.prepare('SELECT * FROM analysis_queue LIMIT 10').all()
  514 |           const wfRows = await db
  515 |             .prepare('SELECT id, name, is_analyzed, analysis_error FROM workspace_files LIMIT 10')
  516 |             .all()
  517 |           return { queueRows, wfRows }
  518 |         })
  519 |         .catch(e => ({ error: String(e) }))
  520 | 
  521 |       console.error(
  522 |         '❌ [Step 05 失败诊断] 队列与工作区文件快照:',
  523 |         JSON.stringify(dbDiagnostic, null, 2)
  524 |       )
  525 |     }
  526 | 
  527 |     // 严格断言：数据不得为空，必须为已完成分析状态
  528 |     expect(analysisRecord).toBeDefined()
```