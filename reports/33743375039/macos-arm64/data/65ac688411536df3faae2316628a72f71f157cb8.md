# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: golden-path.spec.ts >> Release 生产安装包 E2E 黄金主链路验证 >> 05. 单个文件分析结果与属性面板值属性精确检测 (File Analysis & Attributes Inspection)
- Location: pro/tests/e2e/release-pkg/suites/golden-path.spec.ts:304:7

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  421 |     console.log(`[Step 05] 正在轮询等待《${targetFileName}》分析完成 (is_analyzed = 1)...`)
  422 |     let analysisRecord: any = null
  423 |     const maxWaitMs = 120000 // CI 虚拟机环境纯 CPU 本地模型推理预留充足时间
  424 |     const pollStart = Date.now()
  425 | 
  426 |     while (Date.now() - pollStart < maxWaitMs) {
  427 |       analysisRecord = await page.evaluate(
  428 |         async (params: { wsPath: string; fileName: string }) => {
  429 |           const api = (window as any).electronAPI
  430 |           if (!api || !api.getDatabase) return null
  431 |           const db = api.getDatabase()
  432 | 
  433 |           // 1. 查询物理文件记录
  434 |           const wf = await db
  435 |             .prepare('SELECT * FROM workspace_files WHERE name LIKE ? OR path LIKE ? LIMIT 1')
  436 |             .get(`%${params.fileName}%`, `%${params.fileName}%`)
  437 | 
  438 |           if (!wf) return null
  439 | 
  440 |           // 若尚未完成分析或指纹尚未生成，保持等待
  441 |           if (wf.is_analyzed !== 1 || !wf.file_fingerprint) {
  442 |             return {
  443 |               file: wf,
  444 |               tags: [],
  445 |               isCompleted: false
  446 |             }
  447 |           }
  448 | 
  449 |           // 2. 联查 files 与 file_contents
  450 |           let fileData = { ...wf }
  451 |           const contentRow = await db
  452 |             .prepare(
  453 |               'SELECT * FROM files LEFT JOIN file_contents USING(file_fingerprint) WHERE file_fingerprint = ?'
  454 |             )
  455 |             .get(wf.file_fingerprint)
  456 |           if (contentRow) {
  457 |             fileData = { ...fileData, ...contentRow }
  458 |           }
  459 | 
  460 |           // 3. 联查关联的维度标签
  461 |           const tags = await db
  462 |             .prepare(
  463 |               `
  464 |             SELECT t.id, t.name, d.id as dim_id, d.name as dim_name
  465 |             FROM file_tag_relations r
  466 |             JOIN file_tags t ON r.tag_id = t.id
  467 |             LEFT JOIN file_dimensions d ON t.dimension_id = d.id
  468 |             WHERE r.file_fingerprint = ?
  469 |           `
  470 |             )
  471 |             .all(wf.file_fingerprint)
  472 | 
  473 |           // 4. 查询分析队列是否有报错信息
  474 |           const queueError = await db
  475 |             .prepare('SELECT * FROM analysis_queue WHERE file_path LIKE ? ORDER BY id DESC LIMIT 1')
  476 |             .get(`%${params.fileName}%`)
  477 | 
  478 |           return {
  479 |             file: fileData,
  480 |             tags: tags || [],
  481 |             queueItem: queueError || null,
  482 |             isCompleted: true
  483 |           }
  484 |         },
  485 |         { wsPath: workspaceDir, fileName: targetFileName }
  486 |       )
  487 | 
  488 |       if (analysisRecord && analysisRecord.isCompleted) {
  489 |         console.log(`[Step 05] 目标文件在 ${Date.now() - pollStart}ms 内完成处理！`)
  490 |         break
  491 |       }
  492 | 
  493 |       await new Promise(r => setTimeout(r, 1500))
  494 |     }
  495 | 
  496 |     console.log('[Step 05] 数据库提取的最终分析记录:', JSON.stringify(analysisRecord, null, 2))
  497 | 
  498 |     // 如果未完成或有错误，打印深入诊断信息
  499 |     if (!analysisRecord || !analysisRecord.isCompleted) {
  500 |       const dbDiagnostic = await page
  501 |         .evaluate(async () => {
  502 |           const api = (window as any).electronAPI
  503 |           if (!api || !api.getDatabase) return 'electronAPI 不可用'
  504 |           const db = api.getDatabase()
  505 |           const queueRows = await db.prepare('SELECT * FROM analysis_queue LIMIT 10').all()
  506 |           const wfRows = await db
  507 |             .prepare('SELECT id, name, is_analyzed, analysis_error FROM workspace_files LIMIT 10')
  508 |             .all()
  509 |           return { queueRows, wfRows }
  510 |         })
  511 |         .catch(e => ({ error: String(e) }))
  512 | 
  513 |       console.error(
  514 |         '❌ [Step 05 失败诊断] 队列与工作区文件快照:',
  515 |         JSON.stringify(dbDiagnostic, null, 2)
  516 |       )
  517 |     }
  518 | 
  519 |     // 严格断言：数据不得为空，必须为已完成分析状态
  520 |     expect(analysisRecord).toBeDefined()
> 521 |     expect(analysisRecord?.isCompleted).toBe(true)
      |                                         ^ Error: expect(received).toBe(expected) // Object.is equality
  522 |     expect(analysisRecord?.file).toBeDefined()
  523 |     expect(analysisRecord?.file?.name).toContain('项目模块_功能需求文档_日历调度AI集成需求_V1.docx')
  524 |     expect(analysisRecord?.file?.is_analyzed).toBe(1)
  525 |     expect(analysisRecord?.file?.file_fingerprint).toBeTruthy()
  526 | 
  527 |     // 4. 断言分析结果中各关键属性的真实非空性
  528 |     const fileData = analysisRecord!.file
  529 |     expect(fileData.smart_name || fileData.name).toBeTruthy()
  530 |     console.log(`[Step 05] 智能识别/展示名称: ${fileData.smart_name || fileData.name}`)
  531 | 
  532 |     // 验证文本内容提取
  533 |     if (fileData.content) {
  534 |       console.log(
  535 |         `[Step 05] 提取到的文本内容长度: ${fileData.content.length} 字符, 预览: ${fileData.content.slice(0, 100)}...`
  536 |       )
  537 |       expect(fileData.content.length).toBeGreaterThan(0)
  538 |     }
  539 | 
  540 |     // 验证文件类型
  541 |     expect(fileData.type || fileData.name || '').toMatch(/\.docx/i)
  542 | 
  543 |     // 验证维度标签与维度关联
  544 |     const tags = analysisRecord?.tags || []
  545 |     const tagNames = tags.map((t: any) => `${t.dim_name || '未分组'}:${t.name}`)
  546 |     console.log(`[Step 05] 关联的维度标签 (${tags.length} 个): ${tagNames.join(', ')}`)
  547 |     expect(tags.length).toBeGreaterThan(0)
  548 | 
  549 |     // 5. 在 UI 页面中定位并点击该目标文件行/卡片，验证属性面板渲染
  550 |     const targetFileItem = page
  551 |       .locator(`text=${targetFileName}, [title*="${targetFileName}"]`)
  552 |       .first()
  553 |     if (await targetFileItem.isVisible({ timeout: 5000 }).catch(() => false)) {
  554 |       await targetFileItem.click().catch(() => {})
  555 |       await new Promise(r => setTimeout(r, 1000))
  556 |     }
  557 | 
  558 |     const panelLocator = page
  559 |       .locator('aside, [class*="FileDetailsPanel"], [class*="details"], main')
  560 |       .first()
  561 |     await expect(panelLocator).toBeVisible()
  562 | 
  563 |     // 切换到“元数据”或“内容摘要”选项卡若存在
  564 |     const metaTabBtn = page
  565 |       .locator(
  566 |         'button:has-text("元数据"), button:has-text("内容摘要"), button:has-text("Summary"), button:has-text("Metadata")'
  567 |       )
  568 |       .first()
  569 |     if (await metaTabBtn.isVisible().catch(() => false)) {
  570 |       await metaTabBtn.click().catch(() => {})
  571 |       await new Promise(r => setTimeout(r, 600))
  572 |     }
  573 | 
  574 |     await page
  575 |       .screenshot({
  576 |         path: path.join(__dirname, '../reports/html/step-05-single-file-analysis.png')
  577 |       })
  578 |       .catch(() => {})
  579 |     console.log('✅ [Step 05] 单文件分析结果面板与 AI 推理各值属性验证通过！')
  580 |   })
  581 | 
  582 |   test('06. 模型多源下载能力检测 (ModelScope & HuggingFace Qwen3.5-0.8B >1% Progress Smoke Test)', async () => {
  583 |     console.log('--- [Step 06] 验证 ModelScope 与 HuggingFace 模型极速下载探测 (进度 >1% 校验) ---')
  584 |     page = await app.getPage()
  585 | 
  586 |     // 定义待测试的两个轻量模型源：ModelScope 与 HuggingFace
  587 |     const testModels = [
  588 |       {
  589 |         source: 'modelscope',
  590 |         id: 'unsloth/Qwen3.5-0.8B-GGUF:UD-Q4_K_XL',
  591 |         name: 'Qwen 3.5 0.8B (ModelScope)'
  592 |       },
  593 |       {
  594 |         source: 'huggingface',
  595 |         id: 'unsloth/Qwen3.5-0.8B-GGUF:UD-Q5_K_XL',
  596 |         name: 'Qwen 3.5 0.8B (HuggingFace)'
  597 |       }
  598 |     ]
  599 | 
  600 |     for (const item of testModels) {
  601 |       console.log(`\n[Step 06] 开始测试模型下载通道 -> 平台: [${item.source}], 模型: [${item.id}]`)
  602 | 
  603 |       const downloadResult = await page.evaluate(
  604 |         async (modelInfo: { source: string; id: string }) => {
  605 |           const api = (window as any).electronAPI
  606 |           if (!api || !api.modelDownload || typeof api.modelDownload.startDownload !== 'function') {
  607 |             return { supported: false, message: 'modelDownload API not available' }
  608 |           }
  609 | 
  610 |           try {
  611 |             // 1. 发起下载任务
  612 |             const task = await api.modelDownload.startDownload(modelInfo.id, {
  613 |               source: modelInfo.source,
  614 |               autoRetry: false
  615 |             })
  616 |             const taskId = task?.taskId || task?.id || modelInfo.id
  617 | 
  618 |             // 2. 轮询监听下载进度，一旦进度 >1% 或已建立连接接收到有效数据块即算成功
  619 |             let reachedProgress = false
  620 |             let lastProgress = 0
  621 |             const startTime = Date.now()
```